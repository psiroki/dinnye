#!/bin/sh
# Retroarch Ports Script Template

# main configuration :
GameName="Planet Merge"
GameDir="PlanetMerge"
GameExecutable="planets"
GameDataFile=""

# additional configuration
KillAudioserver=1
PerformanceMode=1

# specific to this port :
Arguments=""

# running command line :
XDG_CONFIG_HOME=/mnt/SDCARD/Saves/CurrentProfile/saves /mnt/SDCARD/Emu/PORTS/launch_standalone.sh "$GameName" "$GameDir" "$GameExecutable" "$Arguments" "$GameDataFile" "$KillAudioserver" "$PerformanceMode"
